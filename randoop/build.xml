<project name="Randoop" default="dist" basedir=".">
  <property name="src" location="src"/>
  <property name="build" location="build"/>

  <target name="init">
    <mkdir dir="${build}"/>
    <mkdir dir="${build}/classes"/>
  </target>

  <target name="compile" depends="init">
    <javac srcdir="${src}" destdir="${build}/classes">
      <classpath>
        <fileset dir="lib"/>
      </classpath>
    </javac>
  </target>

  <target name="dist" depends="compile">
    <jar jarfile="${build}/randoop.jar" basedir="${build}/classes"/>
  </target>

  <target name="clean">
    <delete dir="${build}"/>
  </target>
</project>
